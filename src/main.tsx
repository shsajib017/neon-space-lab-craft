
// This file is kept for compatibility
// The main functionality is now in the HTML/CSS/JS structure
console.log("Modern Engineering Systems Laboratory - Initialized");

// Render an empty div since we're using the HTML directly
import { createRoot } from 'react-dom/client';
createRoot(document.getElementById('root') || document.createElement('div')).render(<></>);
